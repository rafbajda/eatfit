import styled from 'styled-components';
import { Text } from 'native-base';
import { deepGrey } from '../../../shared/constants/colors';

export const NotVerifyDividerText = styled(Text)`
    font-size: 20;
    color: ${deepGrey};
`;

export default {
    NotVerifyDividerText
};
