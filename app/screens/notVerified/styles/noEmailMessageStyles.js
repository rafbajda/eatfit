import styled from 'styled-components';
import { Text } from 'native-base';
import { mediumGrey } from '../../../shared/constants/colors';

export const NoEmailText = styled(Text)`
    padding-top: 40;
    font-size: 24;
    color: ${mediumGrey};
`;

export default {
    NoEmailText
};
